# Changelog

## V5.1.0 — 编辑安全版

- 引文序列改为期刊规则：连续序号使用短横线，如 `[1][2] → [1-2]`；不连续序号使用中文逗号，如 `[1][10] → [1，10]`。
- 将 Word 自动编号列表转换为普通文本编号；参考文献统一为普通段落形式，如 `[1] 作者……`，不再保留 Word 列表结构。
- 解析 `numbering.xml` 的实际起始编号，保留诸如从 `2）` 开始的原始编号语义。
- 将 `1）小标题` 与紧随其后的说明段合并为 `1）小标题。正文……`。
- 中文语境中的英文直双引号转换为中文双引号 `“……”`。
- 数字与常见单位之间补空格，如 `150m → 150 m`、`2ms → 2 ms`；普通数值范围的连接符保持原文。
- 多位数字按三位分节，如 `1234.1588 → 1 234.158 8`；排除日期、年份、邮编、基金编号和参考文献年份/页码等高风险区域。
- `参考文献：` 固定加粗；作者简介保持非居中，仅标签加粗。
- 修复标签加粗逻辑，确保“收稿日期：”“基金项目：”等只加粗标签，不误加粗后续内容。
- 增加导出安全闸门：受保护 OOXML 对象数量减少时直接阻止导出。
- 增加输出 DOCX 二次自检：重新打开 ZIP，检查核心 XML 可解析，并核对媒体、嵌入对象和关系文件集合不变；失败时直接阻止下载。
- 用阳领样例完成真实浏览器处理、输出 DOCX 解包校验及 9 页逐页渲染检查。

## V5.0.0 — 重构核心

- 将单文件顺序脚本重构为模块化源码结构。
- 引入 `RuleEngine`，按 package → structure → placement → format → post 五阶段执行规则。
- 引入 `IntegrityGuard`，处理前后核对表格、图片、公式、OLE/对象、域、书签、超链接等结构数量。
- 引入独立 `AuditEngine`，集中输出图表引用、连续引文、参考文献、收稿日期、末级标题、作者简介、颜色残留等质检结果。
- 增加规则执行变更记录。
- 保留已确认规则：日期连接符不擅改、普通数值范围不擅改、连续引文合并、图表题/正文图表引用/文献引文标黄、正文显式颜色归黑、固定“参考文献：”“N  结  论”等。
- 仓库公开版不包含任何真实未发表稿件或黄金样本 DOCX。
